const favLoader = require('./favorites');
const syncLoader = require('./sync');
const accessLoader = require('./access');
const glyphsLoader = require('./glyphs');
const syncedCountLoader = require('./syncedCount');

module.exports = {
  favLoader,
  syncLoader,
  accessLoader,
  glyphsLoader,
  syncedCountLoader,
};
